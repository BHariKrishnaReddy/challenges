#!/bin/python3

import math
import os
import random
import re
import sys


class Employee(object):
    def __init__(self,name,id,age,gender):
        self.name=name
        self.id=id
        self.age=age
        self.gender=gender
class Organisation(Employee):
    def __init__(self, organme,employees):
        self.orgname=organme
        self.employees=employees
    def addEmployee(self,name,id,age,gender):
        self.name=name
        self.id=id
        self.age=age
        self.gender=gender
    def getEmployeeCount(self): 
        return len(employees)
    
    def findEmployeeAge(self,id):
        for id in employees:
            if id == id:
                return age
    def countEmployees(self,age):
        c=0
        for i in employees:
            if i.employees(age) > age:
                c+=1
        return c
    
        
        
        
        
#Enter your code here. Read input from STDIN. Print output to STDOUT

if __name__ == '__main__':
    employees=[]
    o = Organisation('XYZ',employees)
    n=int(input())
    for i in range(n):
        name=input()
        id=int(input())
        age=int(input())
        gender=input()
        o.addEmployee(name,id,age,gender)

    id=int(input())
    age=int(input())
    print(o.getEmployeeCount())
    print(o.findEmployeeAge(id))
    print(o.countEmployees(age))
